# SmolVLA × Go2 双目标导航里程碑计划

最新授权覆盖（2026-09-06）：用户批准将共享GPU例外扩展到DT2并下调安全准入；依据Isaac＋Go2约4.3 GiB实测占用，DT2共享采集启动门槛为实际空闲≥8 GiB、运行余量仍≥2 GiB，仅清理本次进程。DT2完整数据与验收通过后，用户明确授权直接进入DT3，依据训练显存试测调整micro-batch/梯度累积并开始训练；有效batch=16、初始LR=1e-4及DT3累计≤5000 optimizer updates预算不自动扩大。训练与仿真分开，不改任务成功标准。该授权覆盖此前“DT2通过后先等下一条指令”的暂停点，但不授权DT4或以补齐条数代替DT2验收。

日期：2026-09-05。执行者：用户指定的 Codex Terra，推理强度 xhigh。

最新分工覆盖（2026-09-05）：用户要求调整工作方式并继续 DT1，且明确“不需要 terra xhigh 复核”。从本次恢复起由主代理直接实现、调试、测试及验收，不再使用 Terra；下文 Terra 实现/主代理独立验收的旧角色约定由此覆盖。仍保留原始证据重算与全部验收门槛，但不宣称独立代理验收。每个里程碑完成后必须停下，等待用户下一里程碑指令。

执行模式最新更新：用户明确要求每个里程碑完成执行及主代理验收后停下，等待用户下一个里程碑指令。本条取代此前主代理验收后自动推进的约定。Terra xhigh 执行、主代理独立验收的分工保持；当前已下发 DT1 可继续，但通过后不得自动启动 DT2。当前进度以 `reports/dual_target_v1/EXECUTION_STATUS.md` 为准，计划本身不代表实验通过。

## 0. 执行规则与文档优先级

用户已确定：自建双目标场景；视觉与语言决定目标选择；SmolVLA 输出 Go2 高层速度并自主停稳；允许仿真慢于实时；需要 GPU 时可以等待空闲。**Terra 每完成一个里程碑先交主代理验收，主代理通过后停下，必须收到用户下一阶段指令才可再下发。**

本计划是新实验的执行入口。旧 `CODEX_GO2_SMOLVLA_SHORT_VLN_RUNBOOK.md` 和 M6/M7/D5/NaVILA-full 记录仅作审计参考。新实验不继承官方完整 episode、原始长指令、语义 PLY、固定训练步数或墙钟实时性要求。背景见 [前期方案](docs/superpowers/plans/2026-09-05-smolvla-go2-dual-target-stage1.md)。

执行约定：

1. Terra 每次只执行主代理下发的一个阶段，首次为 DT0。主代理独立审查源码、原始证据和必要复验，写入 `DTn_REVIEW.md` 后停下；收到用户下一阶段指令后才下发。
2. 当前阶段内，常规实现、测试、远端同步、诊断修复、该阶段 GPU 等待与有界重试可以继续；不为每次命令请求重复确认。
3. Terra 当前阶段自检完成后写报告、清理本次拥有的进程/锁，交主代理复核。主代理未验收前不得预启动下一阶段作业、预采下一阶段数据或预训练。
4. 阶段未过，不得仅靠测试数量、训练 loss 下降、单条成功视频或修改 `passed` 字段宣布 PASS。
5. 同一失败原因在两次有证据的修复尝试后仍存在，或用完本阶段训练预算，先交主代理诊断。主代理可安排计划范围内的修复；扩大预算、放宽验收或改变关键路线时再找用户。无变化的 GPU 占用属于等待，不算实验失败。
6. 需要新硬件费用、改系统环境、停止其他用户任务、扩大模型/任务范围、放宽验收条件时，先交付证据和具体方案再请用户决定。
7. 只实施本计划相关改动。保留历史代码、数据、checkpoint 和其他用户进程。不要运行全量目录覆盖、`rsync --delete`、全局依赖升级或破坏性 Git 命令。
8. 本地快照和远端各有改动时逐文件比对；以远端运行环境为准，不默认本地更新。为改动文件保存可恢复副本及哈希。新实验源码和小报告同步回本地，大文件留远端。

## 1. 目标与排除范围

第一阶段通过意味着：经过任务微调的 SmolVLA 在指定简单任务分布内，能凭当前 RGB、机体速度和语言指令，控制真实物理仿真的 Go2 选择目标、接近并自主停稳。

不要求通用 VLN、未见物体类别、长程记忆、复杂避障、真实机器人或实时推理。也不能把本实验结论写成这些能力已经得到验证。

首轮固定指令：

```text
Go to the red box and stop in front of it.
Go to the blue box and stop in front of it.
```

不另接颜色检测器、导航器、外部 VLM 或 PD 控制器代替 SmolVLA 选择目标和输出速度。简单真值专家只用于采集与可达性验证。

## 2. 环境定位与已知代码陷阱

| 项目 | 已知位置/事实，DT0 必须重新核对 |
|---|---|
| SSH | `ssh neu-3070`，用户 wxh；现有免密配置已验证，勿索要或记录密码 |
| 远端源码 | `/home/wxh/go2_short_vln` |
| 数据/模型/日志根 | `/mnt/wxh/go2_short_vln` |
| 新实验产物根 | `/mnt/wxh/go2_short_vln/outputs/dual_target_v1/<run_id>/`，尚未创建 |
| 新数据根 | `/mnt/wxh/go2_short_vln/data/dual_target_v1/<dataset_version>/`，尚未创建 |
| 仿真 Python | `/mnt/wxh/go2_short_vln/envs/conda/navila-isaac/bin/python` |
| 学习 Python | `/mnt/wxh/go2_short_vln/envs/conda/smolvla/bin/python` |
| NaVILA 源码 | `/mnt/wxh/go2_short_vln/third_party/NaVILA-Bench`，项目 third_party 下有符号链接 |
| IsaacLab 源码 | `/mnt/wxh/go2_short_vln/third_party/IsaacLab` |
| Go2 配置 | NaVILA 的 `isaaclab_exts/omni.isaac.vlnce/omni/isaac/vlnce/config/go2/` |
| Wrapper | NaVILA 的 `isaaclab_exts/omni.isaac.vlnce/omni/isaac/vlnce/utils/wrappers.py` |
| 硬件 | 最近核实为 RTX 3090 24 GB；别名中的 3070 不是显卡型号 |
| 调度工具 | 本次只读检查发现 `/usr/bin/flock`；`sbatch/qsub` 不在当时 PATH，不能据此断言全机无调度服务 |

复用时必须处理的陷阱：

- Go2 的 `rgbd_camera` 是标准 `CameraCfg`，不必为两个 USD 箱子补语义 PLY；但高度扫描器及其他射线配置仍指向 `/World/matterport`，需要改绑实际平地 mesh 并保持低层观测维度/语义兼容。
- 原 `demo_planner.py` 会渲染红色 expert-path 方块；新 runner 不调用其 Planner 构造器，不把路径绘入策略 RGB。目标箱子本身正常可见。
- 原 `src/inference/isaac_client.py` 按真值距离执行 `evaluator_stop`；新 runner 不能复用该动作分支。
- 原 `full_episode_training.py` 与 converter 强绑定官方 episode schema、50 帧尾段和 terminal sampler cap。只提取适用组件，不能伪造官方 provenance 通过旧校验。
- 原训练报告的“有限 loss”不是闭环验收；原 checkpoint 也不完整保存随机状态、数据采样位置等恢复信息。新入口须落实下文的恢复要求。
- 原机器人配置/Wrapper 可能含 `reference_path`、目标命令生成器和自动 reset。必须追踪实际低层观测及命令来源，证明它只按当前高层速度控制运动，没有隐藏的目标引导。

## 3. 新任务合同

### 3.1 场景与配对设计

一个平地场景，两个形状/尺寸一致、材质分别红蓝的实体箱子，有真实碰撞。首轮从约 1–3 m 的近距离、无遮挡布局开始，固定光照、箱体大小和前视相机，不提前做复杂 domain randomization。

定义 `geometry_group_id` 为独立基础几何组。一个组包含两个目标槽位、起点、初始朝向及派生关系；左右镜像、小扰动、颜色交换、重复轨迹、恢复示范必须继承该组，不得跨 split。不能通过给近重复几何重新编号来绕开分组。

每组具有四个任务：

| 颜色配置 | 指令 red | 指令 blue |
|---|---|---|
| 槽位 A 红、槽位 B 蓝 | 到 A 停稳 | 到 B 停稳 |
| 槽位 A 蓝、槽位 B 红 | 到 B 停稳 | 到 A 停稳 |

首轮保证 A/B 位于起始前方两侧且都可辨认，两条可达路线和停车区域互不冲突。同色跨组同时覆盖两侧及不同距离；不能出现“red 总左转”等偏置。

同一颜色配置的两条指令使用相同初态、复位协议及随机种子。更换指令不能改变目标材质、位置、光照或场景更新顺序；记录复位后状态和首帧图像以验证。颜色交换允许图像变化，但几何/物理参数不变。

2026-09-06 用户批准简化：这里的相同初态指任务条件可比，不要求数值/像素逐位一致。使用 `task_equivalence_v2`：名义场景配置和种子一致；实际 reset 及首次动作前根位置差≤2 cm、完整姿态角差≤3°；每次 reset 历史按既有 wrapper 协议清零，reset 线/角速度范数≤.03 m/s/.05 rad/s，首次动作前平面/偏航速度≤.03/.05。关节细微差异保留记录但不作为跨运行相等门槛。固定目标位姿与物理时钟协议继续核查。图像保留各自 SHA256 验证文件完整性，不比较跨运行 SHA256，也不建立像素差标定专项；检查真实图像有效、目标颜色和位置正确且可见。工程容差在新矩阵前冻结，非按学习器结果放宽。评分条件及16次专家预检数量不变。

停车点/区域定义在箱体固定朝向的前面，所有起点在同一可见接近侧；**不由起点或走过的路径决定停车点**。DT1 根据箱体尺寸、机体前伸长度、相机可见性及碰撞间隙固定区域。保持箱体方向固定，避免给外观对称物体设置视觉上不可判断的“正面”。

### 3.2 策略与低层接口

```text
当前 RGB + [body_vx, body_vy, body_yaw_rate] + task
  -> SmolVLA -> 50×3 action chunk -> 取前 10 帧
  -> 只依赖速度命令和本体/地形观测的 Go2 低层 policy
  -> 仿真推进 -> 新 RGB/机体速度

真值位姿/目标区域 -> 独立专家（采集时）或评分器（评测时）
```

- 策略输入仅允许上述三类；episode ID、时间、绝对位姿、目标坐标/距离/方向、expert command、语义标签不得进入张量或语言 prompt。传输头可含审计 ID，但模型输入构建必须显式 allowlist。
- 输出 `[vx,vy,wz]`；部署 `vy=0`。初始速度边界 `vx∈[0,0.5] m/s`、`wz∈[-0.5,0.5] rad/s`，DT1 实测后冻结。
- 初始采用 50 Hz 的高层命令保持/数据网格，chunk=50，执行 10 帧，仿真时间每 0.2 s 重规划。**50 Hz 不等于物理积分频率，也不等于推理频率**；DT0/DT1 核实 `sim.dt`、decimation、Wrapper.step 实际推进量，并保存物理子步映射。
- 数据记录必须是“动作执行前的 RGB/state -> 随后执行的命令”，不能使用执行后的图像配当前动作。检查相机 update_period、渲染帧更新时间、低层观测历史的复位。
- 推理等待期间物理不推进。允许 `app.update` 维持渲染/通信的前提是验证其不推进物理时钟；否则不得使用。报告墙钟时间，但不设置 200/600 ms 模型能力门。
- 原始动作有限且形状正确才可执行。统一的限幅可用于保护，记录 raw/applied 与限幅情况；不把未执行的 chunk 尾部微小越界直接等同于导航失败。
- 对停车判定检查 raw vx/wz 也接近零，防止大量负 vx 被裁成零而被认作自主停车。固定 vy 通道的映射、归一化及 loss mask 要明确，不能除以零方差。

### 3.3 成功、停车与终止

工程默认：停车点周围 0.3 m，命令 `|vx|<0.03 m/s`、`|wz|<0.05 rad/s`，连续 1 s 仿真时间（50 Hz 下须覆盖 50 个完整时间间隔；按观察时间戳计窗时至少 51 个有效样本，50 个样本仅跨度 0.98 s）。实际机体平移/角速度阈值在 DT1 用真实零命令稳态校准，不能仅检查模型命令。

成功要求同一连续窗口内：raw 与实际执行的 vx/wz 近零；机体实际平移/转动低于阈值；位于正确停车区域；无碰撞/跌倒。机体横向漂移也纳入实际平移速度。初始稳态等待、复制图像、padding 动作不能计入该窗口。

DT1 可根据控制预检校准停车几何、实际速度阈值及 episode 最大仿真时长，生成 `task_contract.lock.json`。建议时长上限先取 30 s，由专家时间确认。DT2 起不得根据学习器失败放宽成功半径或停车阈值；如定义确有错误，需要提交修订依据、版本化并重验受影响阶段。

评分器只能在模型已经满足成功条件后结束 episode，不能先发送零速。异常或超时外部停车记为失败。保持碰撞、跌倒、错误目标停稳、无有效运动、接近后驶离等独立事件与最终失败类别。

### 3.4 数据量与最终统计单位

为覆盖四组合，细化前期草案的计数如下。数字是本计划工程默认，可在相应阶段按诊断增加训练数据；测试数与验收门不得事后放宽。

| 用途 | 独立几何组 | 每组任务 | 初始演示/评测数 |
|---|---:|---:|---:|
| tiny（正式 train 的子集） | 4 | 4 | 16 条演示 |
| train 总集（包含 tiny） | 24 | 4 | 96 条演示，恢复轨迹另计 |
| validation | 8 | 4 | 32 条专家演示及相应闭环任务 |
| final test | 10 | 4 | 40 次冻结 policy 评测 |

40 次最终评测对应 **10 个独立几何组、20 个带颜色的场景配置**，不是 40 个独立样本。报告组合内配对结果；如计算置信区间，按几何组而不是帧/episode 独立重采样。

最终门槛：完整成功至少 32/40；red、blue 各至少 16/20；两种颜色配置各至少 16/20；至少 6/10 几何组四个任务全部成功。后者直接要求同一几何下四种变化均成立。

选 checkpoint 只用 train/validation。测试布局可由专家预检可达性，但不得用于模型训练、normalizer、选步数或策略调参。测试前失败的候选只能依据冻结的几何/可见性/专家可达性规则按组替换，保存拒绝原因；不能看到模型表现后换任务。

## 4. 里程碑总览

| 阶段 | 结果 | GPU | 前置 |
|---|---|---|---|
| DT0 | 环境审计、新实验合同/布局框架、等待与状态入口 | 无 | 主代理下发 DT0 |
| DT1 | 实体双目标、Go2 控制、干净图像、自主停车评分可用 | 有 | DT0 主代理验收通过 |
| DT2 | 16 条 tiny 专家数据与正确的 LeRobot 转换 | 有 | DT1 主代理验收通过 |
| DT3 | tiny 微调与重复闭环，证明任务能被学会 | 有 | DT2 主代理验收通过 |
| DT4 | 分组 train/val/test，正式专家数据与质量审计 | 有 | DT3 主代理验收通过 |
| DT5 | 正式训练、验证选模、模型与测试协议冻结 | 有 | DT4 主代理验收通过 |
| DT6 | 冻结模型的 40 次四组合测试 | 有 | DT5 主代理验收通过 |
| DT7 | 全证据审计、可复现实验报告与演示交付 | 通常无 | DT6 主代理验收通过 |

每阶段必须生成 `DTn_REPORT.md`、`gate.json` 和状态记录。公共 gate 包含阶段、run_id、输入合同/数据/模型哈希、实际命令及退出码、逐项检查、输出路径、失败原因与下一阶段。脚本零退出码是必要条件，不是全部验收。

### DT0 — 审计与 CPU 准备

实施：读取启动 memory 和适用 AGENTS；核对以上环境、源码、资产、低层 checkpoint/normalizer、基础 SmolVLA revision 及依赖；只读追踪 Wrapper 的速度到关节链路、history 和 goal 泄漏点。此阶段禁止启动 Isaac、模型 CUDA 构造或训练。

建立独立 `src/dual_target/` 模块和 `config/dual_target_v1/` 配置；实现纯 CPU 的合同、几何组/四组合生成、分组验证、停车判定核心及状态/等待入口。测试聚焦跨 split 派生泄漏、四组合、禁止策略输入、伪停车/时间窗口与阶段越级，避免为简单格式文件堆积测试。

所有候选在真实相机/碰撞预检前必须标记 `pending_sim_validation`。实现具体 `scripts/dual_target_run_stage.sh DTn` 单阶段入口；只支持已实现阶段，其余明确拒绝，不伪装可运行。生成实际源码/命令清单。

PASS：只读环境报告；关键接口/源码位置已定位；CPU 检查通过；stage 入口不能越级；资源等待 dry-run 不分配 GPU；下一步相机/地形适配路径具体。未知硬件运行行为留给 DT1，不伪称已验证。

交接：提交 DT0 自检结果，等待主代理验收及 DT1 指派。

### DT1 — 最小场景、低层控制与评分

实施：在项目自有 adapter 内创建平地和两个固定箱体，绑定标准前视相机，修正高度扫描器 mesh。复用旧低层 checkpoint 及准确观测顺序、尺度、history；独立 runner 不创建官方路径 marker/目标引导命令。

先测静止、前进、左右转、回零，验证命令方向、实际位移、接触、坐标系及物理时间网格。保存初始/中段/停车 RGB 和俯视视频；俯视信息只能给审计。

实现专家接近/减速/停稳控制和独立评分器。用来自 tiny/train 开发几何的 2 组×4 任务×2 次复位共 16 次专家预检；全部到达并实际停稳。专家允许微调参数至通过，所有失败尝试保留。

PASS 还须包括：两个目标起点可见、目标接近到停止期间可见、颜色交换正确；相同指令以外的配对复位状态一致；真值不流入 learner/低层目标导航；受控碰撞检测能发现碰撞且正常足地接触不误报；真值触发零速不能被评分器认作学习器成功；暂停 1 s 墙钟后物理时间/位姿未推进；锁定任务合同与资源实测。

资源预检包含 Isaac + 已缓存基础 SmolVLA 推理进程共存的峰值显存试运行，不做训练。即使正式训练与 Isaac 分开，闭环阶段二者仍需共存，必须提前验证。临时程序只用于接口/资源测量，不能计为任务成功。

交接：提交 DT1 自检结果，等待主代理验收及 DT2 指派。

### DT2 — tiny 专家采集与转换

实施：固定 4 个 train 几何组的四组合，采满 16 条成功轨迹。尾段记录至少 1 s 真实停稳的新观察/零命令；若低层尚未停稳则继续真实 hold 至满足合同。保存所有原始尝试，训练集只接纳通过检查的成功示范。

转换为新 `dual_target_v1` LeRobot 数据：当前 RGB、3D state、物理 action、task；真值放审计 sidecar。记录帧率、图像变换、episode 分界、chunk/padding mask、train normalizer、采样统计；相邻 episode 不得拼 chunk。图像更新慢于命令网格时如实记录并验证，不得把重复画面当成独立视觉信息。

先保留完整原始数据和终端监督；根据 chunk anchor 的导航/减速/停车曝光决定 sampler。不要直接复制旧 terminal≤10% cap 或恰好50帧尾段要求。默认训练 sampler 先按 episode 均衡，报告实际 terminal/near-zero chunk 比例。

PASS：16/16 演示完整成功；四组合齐全；独立脚本重算时序、状态/动作、终端窗口；真实 loader 抽样回查 RGB 和连续 chunk；normalizer 仅来自 tiny train；视频人工/工具视觉检查无路径/目标提示泄漏。

交接：提交 DT2 自检结果，等待主代理验收及 DT3 指派。

### DT3 — tiny 微调与闭环可学习性

实施：加载固定基础 SmolVLA，创建新 optimizer 与 tiny normalizer；先 expert/state/action projection 可训练。做单 batch 更新、保存重载、固定 observation/noise 对比，证明梯度、更新、mask 和设备正确。

初始训练策略：effective batch=16（micro-batch 按资源调整并记录 accumulation），LR 起始 `1e-4`；使用短预算逐级扩展，累计 optimizer updates 至多 5,000。推荐检查点 250/500/1,000/2,000/5,000，仅在前一档闭环未过且诊断支持继续训练时进入下一档。不是必须跑满；不按毫秒帧数机械推导曝光预算。

先用固定观察和相同噪声进行换指令/换颜色诊断，再跑 16 个 tiny 任务。少量失败分析完成后再扩大到 3 个固定策略种子，共 48 次任务。

PASS：至少 40/48 完整成功；两种指令各≥20/24；每个 tiny 几何组的每个四组合至少成功一次；停车出自模型，raw/applied/实际速度证据齐全。仅训练任务过拟合不代表最终验证完成。

失败路线：先查输入/时序/标签、目标位置偏置和恢复覆盖。允许在这 4 个 train 组内补最多 16 条恢复示范并新建数据版本，计入阶段预算。若任务能拟合但语言响应持续失败，提交具体证据及小范围语言侧适配方案；不要默默重构成导航器或增加真值输入。预算耗尽交付 `DT3 FAIL`，等待用户对新尝试的决定。

交接：提交 DT3 自检结果，等待主代理验收及 DT4 指派。

### DT4 — 正式数据集与 split 冻结

实施：生成 24 train、8 validation、10 test 基础几何组；tiny 的4组属于 train。根据 DT1 冻结规则进行可见性、可达性、分组距离/派生审计；所有四组合继承组 split。

采集 train 96 条、validation 32 条专家演示；验证 test 40 个任务的专家可达性，test 专家数据仅存隔离审计目录，不导出给训练入口。训练恢复示范只来自 train，首轮最多补 32 条，保留失败状态来源与组 ID。

PASS：所有基准专家任务通过冻结标准；train/val/test 派生关系无交叉；数据无渲染/状态/文本泄漏；动作和停车曝光报告完整；train-only normalizer 重算；训练入口拒绝 val/test 更新参数，任何恢复轨迹不改变组归属。

冻结 `split_manifest.json`、`dataset_manifest.json`、`task_contract.lock.json` 与转换代码 hash。阶段内由专家预检规则拒绝的候选保留；不得选取“模型容易成功”的 test。

交接：提交 DT4 自检结果，等待主代理验收及 DT5 指派。

### DT5 — 正式训练、验证选模与冻结

实施：默认重新从同一基础 SmolVLA 开始，使用完整 train normalizer；不直接继承 tiny optimizer/normalizer。tiny run 只提供训练配置与诊断经验。保持 DT3 已通过的输入/动作及参数训练范围。

累计初始预算最多 10,000 optimizer updates，effective batch=16；推荐检查点 500/1,000/2,000/5,000/10,000。每个拟用于选模的 checkpoint 在固定 validation 四组合上评测，预算上限5轮完整验证（每轮32任务）。可先跑固定小子集筛掉明显失败模型，但完整选模必须使用同一32任务。

验证门：完整成功≥26/32；red/blue 各≥13/16；至少4/8组四组合全成功。仅在满足全部验证门的候选中，先按完整成功数选模，同分按全组合成功组数、碰撞数、最后较早 checkpoint 的固定顺序选择，不读取 test policy 结果。

PASS：验证门满足；checkpoint/processor/normalizer 重载可用；明确 trainable 参数；保存模型与协议 hash；冻结 test seed、任务顺序、阈值、代码、失败分类和选模规则。将所有随机种子及 checkpoint 标识写入 `evaluation_freeze.json`。

未通过时保留曲线、动作误差和闭环失败，不启动 DT6。不得仅因为用满预算就挑一个未通过模型继续最终测试。

交接：提交 DT5 自检结果，等待主代理验收及 DT6 指派。

### DT6 — 冻结四组合评测

实施：核验 DT5 freeze 后，对10个test几何组×4组合运行40次 policy 闭环。每组四组合使用相同环境/策略种子，任务顺序预先固定。不在本阶段修改模型、normalizer、输入、动作限幅、停车标准或成功半径。

每个任务保存 RGB、请求/动作、机体状态、评分事件、终止原因、视频、模型 hash；成对生成同几何换指令、同指令换颜色的结果表。允许按完成的 episode 恢复，禁止“多次运行取最好”。

OOM、进程被外部终止、文件损坏等基础设施异常与策略失败分开。基础设施重跑采用原任务/种子并保留异常尝试；策略走错、撞物、超时、提前停不重跑刷成功率。模型返回无效动作属于模型/协议失败，不默默丢弃。

PASS：40个预定任务均有完整试验记录（包括策略失败记录），并满足 §3.4 所有门槛；成功任务自主停车且不存在评分器接管证据。只执行完40次但未达标应记录 `evaluation_completed=true, passed=false`。

失败后立即交付结果；如希望基于 test 失败继续调参，须新建实验版本，该 test 转为开发集，并另建最终 test，不能覆盖本次结论。

交接：提交 DT6 自检结果，等待主代理验收及 DT7 指派。

### DT7 — 证据审计与最终交付

实施：从逐步记录独立重算成功/停车/碰撞/时间窗口，核对模型和数据哈希以及测试无训练使用。审计 DT0–DT6 的实际执行命令、环境版本、复位、禁用目标反馈、RGB 无提示和全部40条任务的完整性。

交付：`FINAL_REPORT.md`（结论和范围）、完整结果 CSV/JSON、10组四组合表、代表性换指令/换颜色/自主停稳视频、全部失败清单、可运行复现命令、数据/模型/视频索引。代表视频选择规则先写明，不能只交最好的一条。

报告独立列出：目标选择、到达、自主停车和完整成功率；碰撞/超时；raw/applied限幅；仿真时间与墙钟耗时。只陈述本任务分布中的结果，明确独立几何组数为10。

PASS：证据链能复核，报告与原始结果一致，当前无遗留占 GPU 的本次任务。发现实质性评测错误则标记受影响阶段待重验，不把文档完成当作模型通过。

停止：交付最终报告，不自动启动新场景、论文实验或实机部署。

## 5. GPU 等待、进程与恢复

2026-09-05 用户已批准一次 DT1 共享 GPU 小规模例外：在空闲 ≥10 GiB（10240 MiB）且连续 3×30 s 采样、持锁即时复查后，允许已有外部计算进程存在，只运行 Isaac + Go2 低层的 50 步烟测（另有原定 100 步预热），不加载 SmolVLA、不训练、不进入 DT2。记录外部 PID 与本次进程显存峰值，运行有界；共享竞争或资源不足时仅停止本次拥有的进程并保留证据。此例外仅改变该烟测的资源准入，不修改任务成功标准，不自动适用于完整专家矩阵、模型共存或后续阶段。旧实验 335 次 GPU 采样峰值为 5637 MiB，只作参考，不视为 DT1 实测需求。其余场合保持下述默认保守策略。

仅在主代理下发的当前阶段需要 GPU 时启动等待。每阶段交接前停止等待器；不得因机器随后空闲绕过主代理验收进入下一里程碑。

优先核实可用的真实调度器；如果没有，使用本项目 `flock` 防止自身冲突，明确它不能预留其他用户的 GPU。首个未测峰值阶段保守等待无其他计算作业且显存足够；允许桌面显示等非计算占用，不要求绝对零显存。

建议初始按可用显存≥20 GiB且无其他计算作业作为首次试运行准入，30–60 s采样一次、连续3次满足并在持锁后立即复核。实际阶段阈值在测得峰值后更新为峰值加余量，记录依据；初始20 GiB只是等待策略，不是模型显存需求或独占保证。

本地/远端等待必须有 `WAITING_GPU` 状态、last_checked、PID、资源快照和可取消入口。持续等待不视为失败；面向用户定期更新。已有等待器先核对锁、PID、启动时间和命令再接续，不重复启动。清理只针对本次登记的 PID/进程组，禁止广义 `pkill python`。

训练与仿真交替运行；闭环所需 Isaac＋SmolVLA 推理共存峰值在 DT1 测量。出现资源竞争/OOM，保存本次日志、卸载自己的资源、退避回等待；重复的内在 OOM 要查配置与测量，不能无限重启。

训练恢复保存 model、processors、optimizer、scheduler（如使用）、AMP scaler（如使用）、Python/NumPy/Torch/CUDA RNG、采样器状态与已消费样本位置、有效 batch、累计 updates。如果无法保证逐步精确恢复，必须明确记录恢复偏差并作为新 continuation，不宣称 bitwise replay。

状态机：`NOT_STARTED -> RUNNING/WAITING_GPU -> READY_FOR_REVIEW -> APPROVED`，异常用 `FAILED` 或 `INTERRUPTED_RESUMABLE`。Terra 只能提交 READY_FOR_REVIEW；主代理独立验收后写 APPROVED 并停下等待用户下一阶段指令。恢复当前阶段不改变阶段编号。验收记录必须绑定输入/输出哈希，不能仅检查一个旧文件存在。

## 6. 建议文件分工与验收记录

2026-09-06 资源授权补充：用户明确要求在当前约18 GB空闲的共享GPU上继续DT1。仅对单次Isaac＋基础SmolVLA非训练共存探针开放`dt1_shared_coexistence_16g_v1`：实际`memory.free`≥16384 MiB、连续3×30 s采样及持锁复查，允许准入时已有外部计算PID。启动、预热、推理期间进程外显存监测，实际余量不足2048 MiB只清理本次拥有的隔离进程组；推理阶段记录双进程共存和外部PID。总墙钟900 s，不执行模型动作、不训练。原20 GiB独占默认及旧10 GiB烟测例外保持独立；此例外不授权DT2，也不降低任务评分。

下列是待实现模块建议，DT0 应结合远端实况确定最小清单；不是现有可运行文件。避免拷贝大量旧 runner 再通过全局 monkey-patch 隐式替换业务规则。

| 位置 | 职责 |
|---|---|
| `config/dual_target_v1/` | 任务默认值、锁定合同、布局和 split 参数 |
| `src/dual_target/contracts.py` / `layouts.py` | 输入/动作/评分合同，四组合与分组 |
| `src/dual_target/scene.py` / `runner.py` | Isaac 场景、低层 adapter、物理时钟、策略运行 |
| `src/dual_target/expert.py` / `scoring.py` | 真值专家；独立评分，禁止反馈到 learner |
| `src/dual_target/collect.py` / `dataset.py` | 真实演示与新 schema 转换 |
| `src/dual_target/train.py` / `evaluate.py` | 训练、恢复、冻结评测；复用适用的 SmolVLA/IPC 组件 |
| `scripts/dual_target_run_stage.sh` | 只执行一个阶段，检查前置和本项目锁 |
| `tests/dual_target/` | 时序、泄漏、停车、split、冻结与恢复等高价值测试 |
| `reports/dual_target_v1/` | 阶段报告和复现命令索引，小文件同步本地 |

每阶段 Terra 向主代理交接：自检 PASS/FAIL、关键量化证据、文件链接、GPU/作业是否已停止。主代理给用户简明验收结果，通过后停止等待用户下一阶段指令；阶段内常规修复自主进行，重要问题或决策请求用户输入。本计划编写本身不算 DT0 PASS。主代理拥有 REVIEW 和 EXECUTION_STATUS 文件，Terra 不得自写独立验收结果。

## 7. Grill-me 审查结论

已通过用户回答收敛的决策：简单双目标足够；允许慢仿真；GPU 可等待；最新授权为 Terra 执行、主代理验收，通过后停止等待用户下一阶段指令，阶段内重要问题再找用户。

已在计划中消除的漏洞：

1. 单次成功无法证明用语言/视觉 → 同几何的颜色交换×指令四组合及整组成功门。
2. 含米数/阶段的旧指令需要隐状态 → 固定视觉目标和与起点无关的停车区域。
3. GT 距离帮忙停 → learner 完成近零命令与实际停稳后评分器才能终止。
4. 颜色=方向或随机拆帧泄漏 → 四组合、基础几何组 split、恢复数据继承组。
5. 有限 loss=训练成功 → tiny 及 validation 均要求真实物理闭环。
6. 训练可独占但推理＋Isaac装不下 → DT1 实测共存峰值。
7. GPU 等待被误当任务失败/无限续跑 → 有状态可恢复等待、阶段 PASS 后明确停机。

剩余未知由对应里程碑解决：新平地对旧低层策略的适配、机体停稳噪声、相机近目标可见性、少量数据可学习性与实际资源占用。它们不是可在计划阶段声称已经通过的事项。
