# 5 Hz ZOH 正式数据集最终验收

结论：APPROVED（仅批准本批专家数据和转换，不代表 SmolVLA 指令跟随已通过）。

数据根：`/mnt/wxh/go2_short_vln/outputs/dual_target_v2/zoh_dataset_0906_v2`。
独立复核根：`/mnt/wxh/go2_short_vln/outputs/dual_target_v2/zoh_dataset_root_review_0906_v2`。

## 验收证据

- 32/32 成功：train 16、validation 8、test 8；按 trajectory/几何组隔离。导出 train 770 帧、validation 412 帧，test 430 个高层观测只在审计原始轨迹，不导出训练格式。
- 主代理独立重放全部专家命令，最大误差 0；50 Hz 状态连续性、200 Hz 物理/50 Hz 低层/5 Hz ZOH、共享限幅与变化率、无碰撞/跌倒/自动重置检查通过。
- 每条末尾 6 个完整真实停稳 hold，共 1.2 秒；完整段计数与日志一致，成功轨迹末尾无不足 0.2 秒段。原 gate 的 partial 段排除测试仍保留。
- 16 组配对 reset 检查通过；采用已批准任务等价容差，不要求逐像素或严格物理初态一致。
- 停车中心误差 0.01154–0.05369 m。
- 16,184 张原始 PNG 哈希登记，1,612 张高层图片全部解码/颜色可见性检查；最少目标颜色像素 1,545，大于 100 阻断线。927 个高层帧有边缘/面积预警，未按预警删帧。
- 主代理实际查看 8 张检查页、160 个面板：覆盖每条起点、两个中段、末尾、最小目标面积帧。指令目标颜色一致；部分右侧目标中途贴近左边缘，仅剩窄条但未完全不可见；末尾下边缘裁切符合近距离停车画面。预警保留，不构成当前阶段阻断。
- 导出 manifest 内 17 个文件哈希全部核验；转换期间真实 LeRobot loader 对起点/中段/尾部的 RGB、状态、5 动作 chunk、padding mask、task 和 episode 边界断言全部通过。主代理额外以 NumPy 独立复算 train-only mean/std，与导出 normalizer 一致。
- 资源中断的 slot 3 attempt 0 已归档，不混入成功轨迹或训练数据；用户批准的一次重试通过，原失败证据保留。
- 队列状态 `DATASET_READY_AWAITING_ROOT_REVIEW` 为队列原始终态，父进程已退出；本报告与批准 sidecar 是主代理最终验收结论，不覆写旧状态。

## 审计工具修正记录

首轮独立颜色计数使用 float64，在 1.15 比例阈值的边界像素处与原 float32 审计计数不一致。复核脚本已显式采用冻结规则的 float32 语义，第二轮全量一致。未更改阈值、源图像或原始审计；首轮输出目录保留。

## 范围与后续

本批可作为后续 SmolVLA 训练数据；不等于模型导航正确性验证，未启动训练。用户已授权的采集提速使用独立版本与训练布局对照，不改本批证据。速度优化不得削弱 50 Hz 停稳/接触状态证据，且必须拆开图像帧序号与状态采样序号后，才能安全降低 RGB 采样频率。
