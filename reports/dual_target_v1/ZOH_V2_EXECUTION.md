# 5 Hz ZOH v2：执行记录

## 最终结果：物理前置 gate APPROVED，已停止

主代理最终验收已完成，见 `ZOH_V2_FINAL_REVIEW.md` 和 `zoh_v2_gate_approval.json`。一条运动/停车探针、四组合专家导航、独立轨迹与图像抽查通过；自有进程退出、锁释放。不代表新版数据集或模型批准；没有继续采集/训练。下文运行与等待记录为历史。

## 最新：用户批准停止旧评估，资源已转交新版

后续确认：新版状态已变为 `RUNNING`，probe/slot 0 子进程 PGID 1955423，Isaac 已完成模型构建阶段并进入首个物理探针流程。此为运行状态，不是探针通过。

2026-09-06 已核对 `/proc/1931071/cmdline` 与旧队列输出路径，并向旧父进程发送 SIGTERM。父进程按原 finally 清理自身 PGID 1953016；随后 ps 确认父进程 1931071 与该子进程均已退出。未删除任何证据，未操作外部 GPU 作业。旧版 7 条已完成（6 超时、1 碰撞），slot 7 中断，不能计入完整失败/成功样本。

新版 PID 1954210 已从 `WAITING_LEGACY_QUEUE` 转为 `WAITING_GPU`，当前任务 probe/slot 0。实际 GPU free 8242 MiB，等待连续采样与持锁复查。下方等待用户答复的记录为历史状态。

2026-09-06，用户授权直接执行新版。当前是控制时序/命令约束的物理前置验证，不是新版数据集或 DT3 验收通过。

## 已实现与验证

- 新增独立 `zoh_*` 模块，旧 runner、锁定合同、旧评估源码和权重不变；远端旧绑定哈希检查通过。
- 物理积分 200 Hz、低层策略 50 Hz；高层 5 Hz，命令仅在每十个低层步的边界更新并保持。默认 chunk=5、execute=1，chunk 可配置。
- 专家和模型分支共用 `ZohClock` / `CommandBoundary`，顺序为有限值检查、绝对限幅、相对上一实际命令的变化率限制、ZOH。复位上一命令为零，vy 固定零。
- 绝对范围保持 vx [0,0.5]、vy=0、wz [-0.5,0.5]；候选变化率 vx=0.5 m/s²、wz=1.0 rad/s²，每 0.2 秒最大变化 0.1 m/s、0.2 rad/s。尚未经物理验收，不能称为已批准阈值。
- 专家仅在高层边界读取真值产生新动作，不是旧轨迹抽帧。`high_level_pre_action.jsonl` 保存 5 Hz 当前 RGB、实测状态、指令、原始/施加命令。原有 50 Hz pre/post 证据和 200 Hz 接触子步保留用于独立审计，不作为 5 Hz 独立训练样本。
- 模型分支只能接收 RGB/速度/语言，并拒绝 chunk、执行步数或 action_dt 不匹配的旧客户端。新版 SmolVLA worker、训练及转换器尚未实现/运行，不宣称模型端已完成端到端验收。
- 新增完整 trajectory / geometry / lineage 的跨 split 拒绝检查；正式 split manifest 尚未冻结。此次探针与四条专家任务均为开发训练用途，不得挪作 validation/test。
- 新版独立停车检查器仅调整命令映射为速率受限 ZOH，保留原始停车请求、实际施加命令、实测速度、区域、接触、连续时长等检查；旧 checker 不修改。
- 新增 8 项测试，本地与远端 Isaac Python 均通过；本地全部 92 项，88 通过、4 项缺少依赖跳过。入口导入和旧锁定绑定远端通过。

## 在跑队列

- 新队列 PID：1954210。
- 输出：`/mnt/wxh/go2_short_vln/outputs/dual_target_v2/zoh_gate_0906_v1/`。
- 队列日志：`/tmp/go2_zoh_gate_0906_v1_queue.log`。
- 启动后确认状态：`WAITING_LEGACY_QUEUE`；未占用 GPU。旧队列 PID 1931071，当时正在 slot 7。
- 用户已被询问是否停止旧评估腾出资源，尚未收到答复；未主动停止旧任务。恢复前先查看两个队列状态和进程，不重复启动。
- 旧队列结束/中断后，再等待实际 free≥8192 MiB、连续 3×30 秒与项目锁复查。运行最低余量 2048 MiB，单任务墙钟上限 1800 秒；只清理自身隔离 PGID。

## 固定验证顺序与停止边界

1. 700 个低层步（14 秒）的固定探针：初始零速、前进、停车、左转、停车、右转、停车。检查真实运动方向，三个停车窗口各取最后 1 秒，要求实测速度低于原冻结阈值，原始/施加命令均为零；无碰撞、跌倒、自动复位。
2. 若探针通过，重新跑 dt1_dev_000 的四个配色/指令专家组合，沿用 30 秒任务时限和真实停车验收。
3. 任一运行/轨迹检查失败立即停止，不重试、不改阈值。五项均通过后状态 `GATE_TRACES_PASSED_AWAITING_ROOT_REVIEW`，队列自动停止，等待主代理查看图像/轨迹后验收。

不会由该队列启动正式数据采集、checkpoint 排序或训练。前置验证未通过前，不冻结变化率候选、不批准数据集。

## 后续仍需完成

- 主代理物理验收、必要的候选参数调整与新版本记录。
- 正式 train/validation/test 基础几何和整 trajectory 分组清单，配色/扰动/恢复派生不得跨 split。
- 全新 5 Hz ZOH 专家采集、完整 hold 处理、边界/padding/停稳样本和 train-only normalizer 的转换验核。
- 新版模型推理/训练接口，默认 chunk 5，并保证共享约束不被第二套不同映射覆盖。
- checkpoint 以验证集闭环任务结果排序；离线诊断只是辅助，测试集不参与选模。
- 本次没有改学习率或扩展训练预算；6,000 步候选仍需在训练计划中明确处理。
