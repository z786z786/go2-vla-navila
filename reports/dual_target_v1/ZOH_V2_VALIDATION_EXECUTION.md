# 新版训练验收与验证集闭环

## 最新：1 GiB运行保护试运行

前次7GiB/2GiB试运行已因free1953MiB停止，未产生完整闭环结果。用户明确授权降低运行保护，本次改1024MiB，仍7168MiB连续准入及1800秒单条上限。独立队列PID2045780，输出 `zoh_val_probe7_floor1_0906_v1`，日志 `/tmp/go2_zoh_val_probe7_floor1_0906_v1_queue.log`。首条任务成功且审计/资源通过才接受该策略用于本矩阵并继续，否则停。原失败目录与源码不覆写；不将资源失败当作导航失败。下文2GiB保护规则为历史版本，不代表当前新试运行的门槛。

## 最新资源试运行授权

用户明确要求降低准入现在尝试，成功后再降低。旧PID2044002在0任务等待状态停止，证据保留。独立 `zoh_val_probe7_0906_v1` 队列PID2044453、父日志 `/tmp/go2_zoh_val_probe7_0906_v1_queue.log`。7168MiB准入、2048MiB运行保护不变。首条1000步checkpoint/validation slot16任务成功且独立审计通过、全程采样未低于保护线，才写本矩阵限定资源批准并继续剩余31任务；否则停止等待用户审查，不自动重试。下文9GiB策略和PID为原队列历史记录。降低准入不等于已降低运行保护，也不等于当前7378MiB已证明足够。

## 训练产物验收：通过，非模型导航批准

用户授权继续验收和闭环验证。已核验1000步正常结束、训练/监督进程退出、首步真实更新与checkpoint/processor重载门槛通过（固定输入噪声预测重载误差0）。1/250/500/750/1000五个checkpoint全部文件哈希重验通过，均为512RGB+3D实测state→3Daction，chunk5/n_action_steps1。所有已记录loss及梯度范数有限。前10/后10记录点loss平均0.71973/0.19928，不作为导航成功或主排序依据。

## 固定验证计划

- 输出 `/mnt/wxh/go2_short_vln/outputs/dual_target_v2/zoh_val_eval_0906_v1`，父PID2044002，日志 `/tmp/go2_zoh_val_eval_0906_v1_queue.log`。
- 四个checkpoint按1000、750、500、250顺序；每个执行validation slots16..23共8条，共32条闭环。两组验证布局，各自四种颜色配置/指令组合。固定policy seed20260906，每条重新初始化模型随机流；scene seed与正式验证轨迹一致。单种子结果不当作多种子稳健性证明。
- 只用validation；test slots24..31显式拒绝，不按离线loss预筛选检查点，不重新采训练数据，不追加训练。
- 入口为独立 `zoh_policy_contract/server/rpc`、`zoh_closed_loop`、`zoh_eval_queue/review`。复用冻结 `zoh_loop` 与 ZOH 共享命令边界，200Hz物理/50Hz低层/5Hz高层，chunk5每次只执行首动作并ZOH保持10低层步。推理等待期间物理时钟不得前进。保留50Hz证据及所有图像；未接入采集提速候选。
- 模型仅收RGB路径（实际编码只读图像）、实测速度和冻结指令，不收目标坐标/评分/episode标签。原始动作不预裁剪，部署由共享限幅/变化率模块处理。
- 停车沿用原raw/applied/实际速度/正确区域连续1秒评分，不强行补零。闭环不是专家数据采集：允许成功或失败在hold中途结束，实际不足段保留为终止证据，不导出训练标签，不要求人为延长为六零段。
- 正常任务超时/碰撞/错停保留失败结果继续固定矩阵；模型进程/通信/文件/资源/时序异常停止，不自动重试。成功声明额外经独立完整停车审计；失败轨迹先做动作/时序/部署一致性审计，最终主代理仍需图像和失败原因复核。
- 队列创建时冻结全部dual_target源码hash及四个checkpoint manifest hash；每条运行前重查。队列运行/等待期间不得修改这些源码。

## 资源与状态解释

Isaac与SmolVLA同时驻留，因此采用独立策略 `zoh_validation_shared_9g_v1`，actual free≥9216MiB连续3次/30秒并持锁复查；运行余量2048MiB，每条墙钟上限1800秒，只清理自有进程组。不能把采集/单独训练的7GiB门槛直接套用到双进程闭环。显存不足时可排队，未获得启动条件不宣称已验证。

本地协议/训练共6项4通过2依赖跳过；远端实际模型环境6项全部通过，涵盖旧50动作拒绝、finite/raw不裁剪、每次新chunk只执行第一步、只允许三项模型输入和chunk5 padding。尚未在Isaac中完成新版真实模型闭环，CPU协议测试不等于闭环通过。

全部32任务结束状态 `MATRIX_COMPLETE_NOT_DT3_APPROVED`，等待主代理验收后比较成功数、错停/碰撞、到达率和停车表现；不自动选择并评估test，不自动继续训练。恢复先读取队列状态和实际PID，不重复启动。
