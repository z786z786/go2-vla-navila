# DT1：双目标仿真、Go2控制与评分验收

状态：**APPROVED（2026-09-06，主代理验收通过）**。仅完成DT1；DT2未开始，等待用户下一里程碑指令。主代理单独实施及复核，不是独立代理验收。批准记录见`dt1_approval.json`和`dt1_final/gate.json`。

## 已完成

- 固定专家矩阵 `dt1_m_0906_v2`：2组几何×2种颜色布局×2条指令×2次复位，16/16成功；无逐项重试或挑选最好结果。全部真实轨迹另脚本重算，均连续实际停稳1.0199999772 s；无目标碰撞、跌倒、外部停车或自动复位。每次用时8.12–9.94 s仿真时间，不包括预热。
- 8对配对复位满足用户批准的task_equivalence_v2：同配置/种子/协议，根位姿差≤2 cm/3°，每次history正确清零并静止起步；不要求关节或图像跨运行逐字节相同。每个图像仍校验自身完整性。
- 16条共7444张真实相机观察经离线颜色/位置检查，初始红蓝均可见、交换位置正确、所选目标未完全出画。已生成16段前视RGB＋俯视轨迹审计视频；俯视面板是GT轨迹重建，非第二台Isaac相机，未反馈给控制。
- 运动校准 `dt1_motion_20260905_r1`：600步/2400物理子步，前进投影0.716922 m，左/右转+0.710142/-0.736158 rad。零命令稳态峰值0.012142 m/s、0.009427 rad/s，保留停车阈值0.03 m/s、0.05 rad/s。
- 红蓝受控碰撞专项分别通过：真实19刚体接触过滤，足地负例目标接触0 N，撞箱正例约1037.414 N并被评分为碰撞失败。详见 `DT1_CONTACT_FIX_REVIEW.md`。
- 全部矩阵每步4×0.005 s物理积分；墙钟暂停≥1 s时物理钟和位姿不变。评分回归拒绝真值触发停车、错误目标停车、非有限值、重复时间和不足1 s的停车窗。
- 模型输入严格限定front RGB、3维机体速度和固定指令，几何/真值只用于专家或审计。低层沿用已核对的45维观测、9帧history及速度命令6:9同步重建，不导入旧导航规划器或路径标记。

## 共存实测与合同冻结

用户授权在当前约18 GB空闲条件下继续后，旧独占等待器已取消并确认退出（未启动GPU）。新run `dt1_coexist_shared_0906_v2` 按单次共享策略准入：实际空闲≥16384 MiB、连续3×30 s采样和持锁复查。实际准入空闲17513 MiB，外部PID1866676占用6550 MiB，保留未操作。启动/预热/推理全程进程外监测，2 GiB运行余量门槛未触发；默认20 GiB独占门槛及旧10 GiB烟测例外不变。

共存实测及原始记录复算通过：60条全程进程外采样，本次拥有进程峰值5618 MiB（5.49 GiB）；整卡采样峰值12267 MiB，实际最低空闲11850 MiB（11.57 GiB）。35条模型阶段采样证明Isaac与SmolVLA同时驻留GPU。基础模型严格加载、新3维接口返回有限[1,50,3]，加载/预处理7.793 s、单次推理0.886 s；模型PyTorch峰值allocated918 MiB/reserved952 MiB。该计时是在共享算力下的一次测量，不是实时性能承诺。

模型等待19.442 s墙钟期间，物理时钟保持manager400/sim405/time2.0249999547，根位姿不变。真实输入RGB人工复查两箱清晰，无GT坐标输入；没有执行模型动作或训练。launcher实际退出0；拥有的等待/Isaac/模型/隔离进程组均已退出，真实项目锁可获取，外部作业仍在。

已生成`config/dual_target_v1/task_contract.lock.json`，锁定0.30 m停车半径、0.75 m箱面stand-off、0.03 m/s及0.05 rad/s实际停车阈值、连续1 s、30 s上限、50 Hz/50 chunk/10执行帧，以及相机、低层和证据哈希。下一阶段必须读取并验证合同，不得因学习失败放宽标准。当前停止，不启动DT2。

共享补丁归档SHA256：`114e8d310b1617ee59c010bfa8c972ad3d70eb7e678858b37f793fdedf7d6226`；远端旧文件备份在`dt1_shared_coexist_deploy_0906/before.tar.gz`。16任务矩阵运行期间源码完全冻结；矩阵完成后只新增共存探针/保护并修改GPU准入，不改环境、专家或评分源码。旧shared-smoke脚本的未部署本地候选不属于本次执行快照。

## 限制和证据边界

人工抽查最少可见帧发现，蓝目标在某些中途观察仅在画面边缘露出窄条（最低约2064个颜色像素）。这满足本次“未完全出画”的检查，但不代表目标完整、居中或始终易辨认；后续tiny学习是否受影响尚未知。此限制不通过修改图像、隐藏真值或放宽停车评分来掩盖。

专家使用真值完成任务，因此16/16只证明环境、速度控制和专家/评分链路可用。基础SmolVLA接口/共存通过，也不能证明语言选目标、跟随位置变化或自主停稳；这些须以后续获授权的微调与闭环验证为证。

最终共享补丁回归共71项：本地69通过、2项因缺真实OpenCV/LeRobot依赖跳过；远端Isaac环境70通过、仅SmolVLA处理器因环境依赖跳过。该处理器已在远端SmolVLA专用环境单独通过，最终真实GPU推理再次覆盖它。CPU命令显式禁用CUDA。审计器合成自测拒绝10种无效轨迹，真实运动及红蓝接触原始记录再次复算通过。

日志保留旧Go2视觉网格UV和headless GLFW告警；未发现本次Error/Traceback，实际相机图像有效、推理和物理审计均完成，不将告警描述为零。显存查询已修为读取驱动实际`memory.free`，避免总量减已用量忽略约460 MiB驱动预留。

## 证据位置

- 远端根目录：`/mnt/wxh/go2_short_vln/outputs/dual_target_v1/`。
- 矩阵：`dt1_m_0906_v2/{expert_matrix_plan.json,expert_matrix_result.json,root_matrix_review.json}`，原始每条记录在同前缀16个run目录；root重算绑定96个证据文件哈希。
- 视觉：`dt1_m_0906_v2_visual/{visual_review.json,00.mp4,...,15.mp4}`。本地索引 `dt1_matrix_visual_review.json`。
- 共存：`dt1_coexist_shared_0906_v2/`，包含原始图像、模型请求/结果、资源策略、双进程采样、源码快照及`root_coexistence_review.json`；启动全程采样与launcher日志位于输出根同名前缀文件。
- 历史失败、修复归档、专项报告均保留，详见 `DT1_CONTINUATION_REVIEW.md` 和 `DT1_REVIEW.md`；没有追认失败尝试通过。
