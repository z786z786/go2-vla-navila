# P3-T1C Leakage determination

Status: **UNVERIFIABLE / MISSING**. The required ranged probe produced 2,097,152 bytes, but the JSON is truncated and cannot establish schema. Resumable download attempts reached 4,308,992 bytes before repeated `curl: (35) SSL routines::unexpected eof while reading` failures. The required 317,267,756-byte file and SHA-256 were therefore not obtained.

No scene intersection, record count, instruction/action fields, or example records are reported because doing so would require parsing an incomplete file and could produce a false NO-LEAKAGE verdict. The partial artifact is at `/mnt/wxh/go2_short_vln/downloads/navila_probe/R2R_annotations.json`.
