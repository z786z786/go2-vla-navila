# Public release preflight (draft)

Generated 2026-09-13 before any cleanup or push.

## Candidate content

- `go2_short_vln_manifest.txt`: 517 source/config/test files from this repository.
- `llada_go2_platform_manifest.txt`: 40 platform, adapter, tool, and configuration files from `/home/wxh/llada-vla-go2`.
- `README.public.draft.md`: proposed public-facing README.

## Findings

The core implementation is broad enough to retain the source tree, navigation conversion, evaluation modules, action interfaces, tests, and deployment adapters. Large datasets, checkpoints, caches, generated bytecode, and private experiment records should be excluded during packaging.

The initial keyword scan is intentionally over-inclusive. It found 369 matches, including ordinary words such as `token`, test fixtures, historical reports, and one explicit private SSH command in `reports/SESSION_HANDOFF.md`. Every match requires classification before release; the report is not a clearance.

Known private material requiring exclusion or redaction includes remote host/port commands, machine-specific paths, credentials if present in ignored/untracked files, `.pyc` files, archives, raw datasets, and training outputs. The public README draft does not include private endpoints or credentials.

## Next packaging step

Build a clean staging tree from the manifests, apply license headers and upstream notices, copy only approved media, rerun secret/history scans, then review the staged diff before updating the GitHub repository.
