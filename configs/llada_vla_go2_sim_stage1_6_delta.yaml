model:
  model_path: null
  model_name: llada-vla-go2-sim-stage1-4-delta-lora-last4
  bf16: true
  attn_implementation: flash_attention_2
  device_map: auto
  init_checkpoint_path: null
  reset_action_modules_after_load: true

data:
  train_data_path: null
  eval_data_path: null
  image_folder: null
  image_aspect_ratio: square

velocity_control:
  horizon_k: 4
  chunk_size: 4
  control_hz: 10.0
  n_bins: 256
  action_bins: 256
  vx_range: [-1.0, 1.0]
  vy_range: [-0.6, 0.6]
  wz_range: [-1.4, 1.4]
  action_target_mode: delta
  use_residual_action: false
  anchor_mode: repeat_prev
  use_prev_action_as_condition: true
  anchor_use_dropped_prev_action: false
  use_step_level_remask: true
  step_level_remask_aggregation: mean_prob
  per_dim_action_heads: true
  loss_w_vx: 1.0
  loss_w_vy: 1.0
  loss_w_wz: 1.2
  delta_vx_range: [-0.35, 0.35]
  delta_vy_range: [-0.20, 0.20]
  delta_wz_range: [-0.50, 0.50]
  absolute_action_ranges:
    vx: [-1.0, 1.0]
    vy: [-0.6, 0.6]
    wz: [-1.4, 1.4]
  residual_action_ranges:
    vx: [-0.35, 0.35]
    vy: [-0.20, 0.20]
    wz: [-0.50, 0.50]
  fallback_image_timeout_s: 0.5
  ema_alpha: 0.25
  max_delta_vx: 0.35
  max_delta_vy: 0.20
  max_delta_wz: 0.50
  allowed_locomotion_modes: [1, 2, 3, 4]

masking:
  mode: high_mask_curriculum
  schedule:
    - until: 0.20
      mask_ratio: [0.5, 0.7]
      full_mask_probability: 0.30
    - until: 0.60
      mask_ratio: [0.7, 0.9]
      full_mask_probability: 0.40
    - until: 1.00
      mask_ratio: [0.85, 0.95]
      full_mask_probability: 0.50

previous_action_dropout:
  enabled: true
  zero_probability: 0.15
  mask_probability: 0.10
  noise_probability: 0.10
  noise_std: 0.02
  quantize_probability: 0.25
  quantize_decimals: 1

optimization:
  train_mode: projector_only
  freeze_vision_tower: true
  freeze_llada_backbone: true

lora:
  enabled: true
  r: 64
  alpha: 16
  dropout: 0.05
  bias: none
  last_n_layers: 4
  layers_pattern: layers
  target_modules: [q_proj, k_proj, v_proj, o_proj, gate_proj, up_proj, down_proj]

training:
  output_dir: outputs/llada_vla_go2_sim_stage1_4_delta_lora_last4
  logging_dir: outputs/llada_vla_go2_sim_stage1_4_delta_lora_last4/runs
  per_device_train_batch_size: 1
  per_device_eval_batch_size: 1
  gradient_accumulation_steps: 32
  max_steps: 512
  learning_rate: 1.0e-4
  weight_decay: 0.01
  warmup_ratio: 0.03
  logging_steps: 4
  save_strategy: steps
  save_steps: 128
  evaluation_strategy: "no"
  save_total_limit: 2
  load_best_model_at_end: false
  bf16: true
  fp16: false
  gradient_checkpointing: false
  remove_unused_columns: false
  dataloader_num_workers: 0
  report_to: [tensorboard]
